#test block
    lw $t0, ($s0)  # Load the value at the memory address in $s0 into $t0
    li $v0, 1      # Set $v0 to 1 (print integer)
    move $a0, $t0  # Load the value from $t0 to $a0
    syscall        # Perform the system call to print the integer
    lw $t0, 4($s0)  # Load the value at the memory address in $s0 into $t0
    li $v0, 1      # Set $v0 to 1 (print integer)
    move $a0, $t0  # Load the value from $t0 to $a0
    syscall        # Perform the system call to print the integer
    lw $t0, 8($s0)  # Load the value at the memory address in $s0 into $t0
    li $v0, 1      # Set $v0 to 1 (print integer)
    move $a0, $t0  # Load the value from $t0 to $a0
    syscall        # Perform the system call to print the integer
    lw $t0, 12($s0)  # Load the value at the memory address in $s0 into $t0
    li $v0, 1      # Set $v0 to 1 (print integer)
    move $a0, $t0  # Load the value from $t0 to $a0
    syscall        # Perform the system call to print the integer
    lw $t0, 16($s0)  # Load the value at the memory address in $s0 into $t0
    li $v0, 1      # Set $v0 to 1 (print integer)
    move $a0, $t0  # Load the value from $t0 to $a0
    syscall        # Perform the system call to print the integer